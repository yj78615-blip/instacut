# InstaCut — 텍스트 → 인스타 컷툰 생성·수정·배포 파이프라인 PRD

- 문서 버전: v1.0 (M0 완료 — 실제 구현 결과 반영)
- 작성일: 2026-08-14
- 상태: 검토 대기

---

## 1. 문제 정의

인스타 컷툰(4~10컷 캐러셀 만화)을 한 편 만드는 데 드는 시간은 대부분 **그림 그리기**가 아니라 다음 세 가지에 소모된다.

1. 원고 텍스트를 몇 컷으로 나눌지, 각 컷에 어떤 그림과 대사를 넣을지 정하는 **컷 분할**
2. 컷마다 **같은 캐릭터·같은 화풍**을 유지하는 일
3. 마음에 안 드는 **한 컷만 다시 고치는** 일 — 보통 전체를 다시 만들게 된다

InstaCut은 원고 텍스트 한 덩어리를 넣으면 컷 단위로 쪼개고, 컷 단위로 이미지를 만들고, **컷 단위로 다시 만들 수 있는** 상태를 유지한 채 인스타 캐러셀로 내보내는 파이프라인이다.

---

## 2. 목표 / 비목표

### 목표

| ID | 목표 |
|---|---|
| G-1 | 원고 텍스트 → 인스타 업로드 가능한 컷툰 이미지 세트를 **사람 개입 없이 1회 생성** |
| G-2 | 특정 컷만 **다른 컷에 영향 없이** 재생성·수정 |
| G-3 | 한 편 전체에서 **캐릭터 외모와 화풍 일관성** 유지 |
| G-4 | 한글 대사가 **깨지지 않고 읽히는** 상태로 합성 |
| G-5 | 완성본을 인스타 캐러셀로 게시 (수동 → 자동 순서로 단계화) |

### 비목표 (이번 범위 아님)

- 원고 자체를 창작하는 것 (원고는 사용자가 제공)
- 동영상·릴스·애니메이션
- 다국어 (한국어 전용)
- 다중 사용자 SaaS, 계정 시스템, 결제
- 웹툰 플랫폼(네이버·카카오) 규격 대응

---

## 3. 사용자와 시나리오

**주 사용자**: 자기 사연·에세이·정보성 글을 컷툰으로 올리는 1인 운영자.

**핵심 시나리오**

1. 오늘 쓴 사연 텍스트 800자를 붙여넣는다
2. "8컷으로" 지시 → 시스템이 컷 스크립트(대사·화면 지시)를 뽑아 보여준다
3. 3번 컷 대사가 어색해서 직접 고친다
4. 생성 실행 → 8장 완성
5. 5번 컷 그림이 마음에 안 든다 → 5번만 재생성 (seed 변경 또는 프롬프트 수정)
6. 최종 확인 후 캐러셀 게시

---

## 4. 핵심 설계 원칙

이 네 가지가 품질을 결정한다. 나머지는 부수적이다.

### P-1. 한글 텍스트는 절대 이미지 모델에 맡기지 않는다

현재 이미지 생성 모델은 한글을 제대로 못 쓴다. 대사·나레이션은 **투명 배경 그림 위에 텍스트 레이어로 합성**한다. 이미지 모델에는 "말풍선 자리를 비워둔 그림"만 요청한다.

→ 컷 이미지 생성과 말풍선 렌더링은 **완전히 분리된 두 단계**이고, **시점도 다르다.** 그림은 [3]에서 만들고, **텍스트는 [4] 수정 단계에서 넣는다.** 그림을 보기 전에는 말풍선을 어디에 얼마나 크게 놓을지 정할 수 없기 때문이다.

### P-2. 컷은 독립적으로 재생성 가능해야 한다

각 컷은 자신을 다시 만들 수 있는 모든 정보(프롬프트, seed, 캐릭터 참조, 파라미터)를 스스로 들고 있어야 한다. 컷 5를 다시 만들 때 컷 1~4를 건드릴 이유가 없어야 한다.

→ 프로젝트 상태는 **컷 배열 JSON 한 파일**에 저장한다.

### P-3. 일관성은 한 편 안에서만 필요하다

단발 제작이므로 편과 편 사이의 일관성은 요구사항이 아니다. **한 편 = 8컷 안에서만** 캐릭터와 화풍이 유지되면 된다.

이를 위해 편 시작 시 **화풍 문자열과 캐릭터 묘사 문자열을 확정**하고, 모든 컷 프롬프트에 기계적으로 똑같이 주입한다. 컷마다 다시 쓰지 않는 것 자체가 일관성 장치다.

### P-5. 한국어 → 영어 번역은 직역이 아니라 시각 요소 분해다

사용자는 한국어로 화풍을 말한다("감성적인 그림체"). 이미지 모델은 영어를 받는다. 그런데 여기서 필요한 건 번역이 아니라 **변환**이다.

- "감성적인" → `emotional` 은 이미지 모델에 **아무 효과가 없다**. 화면에 그릴 게 없는 단어다
- `soft diffused lighting, warm color grading, muted palette` 로 **관측 가능한 시각 요소로 분해**해야 그림이 바뀐다

한국어 화풍 표현은 대부분 인상어(감성적인, 담백한, 몽글몽글한)다. 이걸 선·색·빛·구도의 영어 서술로 푸는 것이 이 계층의 실제 일이다.

**그리고 그 변환 결과를 사용자에게 반드시 보여준다.** 의도와 다르게 풀렸을 때 고칠 수 있는 유일한 지점이고, 여기서 못 고치면 8컷을 다 만든 뒤에야 알게 된다.

### P-6. 시선은 좌우 반전 S 자로 흐른다 — 말풍선 → 그림 → 말풍선

컷 안에서 독자의 눈은 왼쪽 위에서 시작해 **좌 → 우 → 좌 → 우로 번갈아 가며 계단식으로 내려간다.** 세로로 늘인 S 자 곡선을 그린다.

```
     ①  ←── left-upper     (y 0.03)
        ②  ←── right-upper  (y 0.26)
     ③  ←── left-lower     (y 0.50)
        ④  ←── right-lower  (y 0.73)
```

### 그림도 흐름의 한 지점이다

말풍선만 배치하는 것이 아니다. **인물이 말풍선들 사이에 놓여야** 곡선이 완성된다.

> 말풍선 → **그림** → 말풍선

그래서 두 번째 이후 말풍선에는 두 조건이 붙는다.

1. **인물 머리보다 아래 자리**에 놓는다 — 그림을 지나야 하므로
2. **직전 말풍선과 좌우 반대편**을 우선한다 — 곡선이 되도록

이걸 빼먹으면 말풍선들이 인물 위쪽에 나란히 몰리고, 인물이 흐름의 중간이 아니라 끝에 놓인다. 실제로 그렇게 만들었다가 시선이 어긋났다.

### 두 가지 함정

**좌우가 같은 높이면 안 된다.** 자리를 2×2 격자로 두고 좌·우를 같은 y에 놓으면 시선이 가로로 움직여 S 자가 아니라 Z 자로 꺾인다. 자리마다 높이가 한 단씩 낮아져야 한다.

**되돌아가면 안 된다.** 앞 자리가 막혀 건너뛰더라도 순서는 항상 아래로만 간다. 위로 되돌아가면 시선이 역행하고, 한 단계씩 내려가야 급격한 점프가 없다.

### 자리를 고정하는 이유

그림의 빈 곳을 찾아 그때그때 놓는 방식도 써봤지만, 컷마다 말풍선이 다른 데 붙어 캐러셀 전체가 산만해졌다. **자리가 예측 가능한 편이 낫다.**

이 순서를 흔드는 것은 P-7(머리 회피) 하나뿐이다.

### P-7. 말풍선은 인물을 덮지 않는다

**이 규칙이 P-6보다 우선한다.** 시선 흐름이 원칙이지만, 그 자리에 인물이 있으면 자리를 포기한다.

컷툰에서 독자가 가장 먼저 보는 것은 인물이고, 표정이 곧 내용이다. 얼굴이 가려진 컷은 대사를 아무리 잘 배치해도 실패한 컷이다.

배치 우선순위는 이 순서로 고정한다.

1. **인물과 겹치지 않는다** (절대 조건 — 이걸 못 지키면 나머지는 의미 없다)
2. 시선 흐름 순서: 첫 말풍선은 위, 다음은 아래+반대편 (P-6)
3. 읽기 순서: 좌 → 우

세 조건을 모두 만족하는 자리가 없으면 **1번을 지키고 2번을 버린다.**

**얼마나 엄격히 피할지는 좌표를 얼마나 믿느냐에 달렸다.** 인물 전체를 정확히 알면 바짝 피하고, 얼굴만 알면 몸에 걸치는 것은 허용하고, 추측일 뿐이면 느슨하게 본다. 추측 때문에 시선 흐름을 깨는 것은 과하다 — 실제로 그렇게 만들었다가 말풍선이 전부 아래로 몰린 적이 있다.

| 좌표 출처 | 아는 것 | 회피 강도 |
|---|---|---|
| 모델 질의 | 인물 몸 전체 | 엄격 |
| 얼굴 검출 | 얼굴만 | 보통 |
| 가정값 | 추측 | 느슨 |

이 규칙은 "복잡한 영역을 피한다"는 것과 다르다. 인물이 어디 있는지 실제로 알아야 지킬 수 있고, 배경이 복잡하고 인물이 단순한 컷에서는 복잡도만으로는 오히려 인물 쪽을 고르게 된다.

### P-4. 게시 후 이미지는 못 고친다

인스타는 게시된 게시물의 **캡션은 수정 가능하지만 이미지는 교체 불가**다. 이미지를 고치려면 삭제 후 재게시뿐이고, 그러면 좋아요·댓글이 사라진다.

→ **게시 전 검수 단계가 되돌릴 수 없는 관문**이다. 이 단계를 생략 가능하게 만들면 안 된다.

---

## 5. 파이프라인

```
원고 텍스트
   │
   ├─ [1] 해석·번역      원고 + 화풍(한국어) → 컷 스크립트 JSON   ★ LLM 호출 1회
   │                      ├ 컷 분할 (장면 + 대사)
   │                      ├ 화풍 한국어 → 영어 프롬프트 변환
   │                      ├ 캐릭터 묘사 추출 → 영어 변환
   │                      └ 컷별 장면 → 영어 프롬프트
   │
   ├─ [2] 검토           번역 결과와 컷 스크립트를 사람이 확인·수정
   │                      ↑ 되돌리는 비용이 가장 싼 유일한 지점
   │
   ├─ [3] 컷 이미지 생성   컷별 프롬프트 → **그림만** (왼쪽 여백 확보, 텍스트 없음)
   │
   ├─ [4] 검수·수정      그림 확인 → **말풍선 텍스트 삽입** → 컷 재생성 → 반복
   │                      ↑ 텍스트 합성은 여기서 일어난다. 생성 직후가 아니다
   │
   └─ [5] 배포          캐러셀 업로드 + 캡션·해시태그
```

각 단계는 **독립 실행 가능**해야 한다. 3번 컷의 그림만 다시 만들기, 5번 컷의 텍스트만 다시 얹기가 모두 가능해야 한다.

**[3]과 [4]를 나눈 것이 이 제품의 작업 리듬을 결정한다.** 생성은 느리고(GPU) 합성은 빠르다(1초). 텍스트를 고칠 때마다 그림을 다시 만들 이유가 없다.

번역을 별도 단계로 두지 않고 [1]에 합친 이유: 컷 분할 LLM이 어차피 원고를 읽고 영어 프롬프트를 쓰고 있다. 화풍·캐릭터 번역을 같은 호출에 얹으면 **추가 API 호출이 0회**다. 게다가 원고 맥락을 아는 상태로 번역하므로 결과도 더 낫다.

---

## 6. 데이터 모델

프로젝트 하나 = 디렉터리 하나 + `project.json` 하나.

```
projects/2026-08-14-첫출근날/
├── project.json
├── raw/                     # [3] 산출물: 텍스트 없는 그림 (왼쪽 여백 확보)
│   ├── cut_01.png
│   └── ...
└── out/                     # [4] 산출물: 말풍선 얹은 최종 컷
    ├── cut_01.png
    └── ...
```

```jsonc
{
  "title": "첫 출근 날",
  "source_text": "원고 원문 전체",
  "style": {
    // 사용자 입력 원문 — 항상 보존한다. 영어만 남기면 나중에 의도를 알 수 없다
    "art_style_ko": "심플한 라인 드로잉, 파스텔 톤, 감성적인 느낌",
    "character_ko": "20대 여성, 단발, 검은 뿔테 안경, 베이지 코트",

    // [1]이 변환한 결과 — 사용자가 여기를 직접 고칠 수 있다
    "art_style_en": "clean minimal line art, thin uniform linework, soft pastel palette, low saturation, soft diffused lighting, generous white space",
    "character_en": "young woman in her 20s, short bob haircut, black horn-rimmed glasses, beige trench coat",

    "aspect_ratio": "4:5"           // 캔버스 1080x1350. 그림이 전체를 채운다
  },
  // projects/<제목>/character_ref.png 가 있으면 그 캐릭터로 전 컷을 그린다.
  // 텍스트 묘사만으로는 "이 캐릭터"를 재현할 수 없다 — no nose 라고 써도 코를 그린다.
  "caption": "게시물 캡션",
  "hashtags": ["#컷툰", "#일상"],
  "cuts": [
    {
      "index": 1,
      "beat": "지하철에서 긴장한 채 서 있다",   // 컷 분할 결과 (한국어)
      "scene_en": "standing in a crowded subway car, nervous tense expression, holding a handrail",
      // 실제로 모델에 들어간 문자열 전체. 생성 시 조립되어 기록된다 (읽기 전용)
      "final_prompt": "clean minimal line art, ... | young woman in her 20s, ... | standing in a crowded subway car, ...",
      "negative_prompt": "text, letters, speech bubble, watermark, ...",
      "seed": 771823,
      "balloon_zone": "left",        // 그림에서 비워둘 영역. 기본값 left (P-6)
      // 인물이 차지하는 영역(몸 전체). 말풍선은 여기를 침범하지 않는다 (P-7).
      // [4] 에서 한 번 구해 기록하므로 재합성은 API 를 다시 부르지 않는다.
      // 틀렸으면 직접 고칠 수 있고, 고친 값은 덮어쓰지 않는다.
      "subject_box": [0.42, 0.46, 0.61, 0.67],   // x0, y0, x1, y1 (캔버스 대비 비율)
      // pos 미지정 시 좌상단부터 순서대로 자동 배치. [4]에서 실제 좌표가 채워진다
      "texts": [
        { "type": "narration", "content": "첫 출근 날 아침.", "pos": "top" },
        { "type": "dialogue",  "content": "심장 터질 것 같아...", "pos": "left-upper" }
      ],
      "raw_image": "raw/cut_01.png",
      "out_image": "out/cut_01.png",
      "locked": false                // true면 일괄 재생성 시 건너뜀
    }
  ]
}
```

**`locked` 필드가 P-2의 실질적 구현이다.** 마음에 든 컷을 잠그면 전체 재생성을 눌러도 살아남는다.

**`_ko` / `_en` 쌍과 `final_prompt`이 P-5의 실질적 구현이다.** 사용자는 한국어만 쓰고, 영어를 읽고 싶으면 읽고, 고치고 싶으면 고친다. `final_prompt`은 "실제로 모델에 뭐가 갔는가"에 대한 완전한 답이라 컷이 이상하게 나왔을 때 원인을 추측할 필요가 없다.

---

## 7. 기능 요구사항

### F-1. 컷 분할 (원고 → 컷 스크립트)

- 입력: 원고 텍스트, 목표 컷 수(기본 8, 범위 4~10), **화풍 문자열(한국어)**
- 출력: `style` 블록(`_ko`/`_en` 쌍) + `cuts[]` 배열 — **LLM 호출 1회로 전부**
- 요구사항
  - 마지막 컷은 마무리·훅 역할을 하도록 지시 (캐러셀 이탈률 방어)
  - 컷당 대사 총 길이 상한 (기본 40자) — 넘으면 말풍선이 그림을 덮는다
  - **대사·나레이션은 한국어 원문 그대로. 절대 번역하지 않는다** (F-4에서 그대로 렌더링)
  - **장면·화풍·캐릭터만 영어로 변환** (F-2 규칙 적용)
- 검증: 컷 수 일치, 모든 컷에 `scene_en`과 최소 1개 text 존재, `texts[].content`에 영문이 섞이지 않았을 것

### F-2. 스타일 번역·고정

#### F-2a. 한국어 → 영어 변환 규칙

화풍은 편 생성 시 사용자가 **한국어로** 지정한다 — `--style "심플한 라인 드로잉, 파스텔 톤, 감성적인 느낌"`.
캐릭터 묘사는 [1]이 원고를 읽고 한국어로 추출한 뒤 함께 변환한다.

변환 시 [1]에 강제하는 규칙:

| 규칙 | 내용 |
|---|---|
| R-1 | **인상어는 시각 요소로 분해한다.** 화면에 그릴 수 없는 단어를 영어로 옮기지 않는다 |
| R-2 | **한 인상어 → 2~4개 시각 요소.** 선·색·빛·구도·질감 중 해당하는 축으로 푼다 |
| R-3 | **원문에 없는 요소를 추가하지 않는다.** "파스텔 톤"에서 `cherry blossom background`가 나오면 안 된다 |
| R-4 | 이미지 모델이 실제로 반응하는 관용 표현을 쓴다 (`cel shading`, `flat colors`, `soft diffused lighting`) |
| R-5 | 결과는 쉼표로 구분된 명사구 나열. 문장으로 쓰지 않는다 |

변환 예시는 **부록 B** 참조.

#### F-2b. 변환 결과 공유

- `art_style_ko` / `art_style_en`을 **나란히 저장.** 한국어 원문을 절대 버리지 않는다
- 생성 실행 전 콘솔에 한국어-영어 대조를 출력하고 **사용자 확인을 받는다**
- 사용자는 `project.json`의 `_en` 값을 직접 고칠 수 있다. 고치면 그 값이 그대로 쓰인다 (재번역하지 않음)
- 컷 생성 시 조립된 문자열 전체를 `final_prompt`에 기록 — 결과가 이상할 때 추측하지 않기 위해

#### F-2c. 고정

- `art_style_en`과 `character_en`은 **모든 컷 프롬프트 앞에 기계적으로 동일하게 붙는다.** 컷별로 변형하지 않는다
- **프리셋·템플릿 시스템 없음.** 잘 먹힌 조합은 그 편의 `project.json`에 남으니 다음 편에 복사해 쓰면 된다
- 참조 이미지 방식(IP-Adapter)은 **M0 결과를 보고 판단.** 문자열 고정만으로 충분하면 넣지 않는다

### F-3. 컷 이미지 생성 (그림만)

- **산출물은 `raw/`뿐이다. 텍스트를 얹지 않는다.** 합성은 F-4가 [4] 단계에서 한다
- **왼쪽 여백 강제 (P-6)** — 모든 컷 프롬프트에 고정 주입:
  `subject positioned on the right side of the frame, empty uncluttered space on the left third, simple background on the left`
- `balloon_zone`이 `left`가 아닌 컷은 그에 맞는 여백 지시로 교체 (예외 컷용)
- 네거티브 프롬프트 고정 포함: `text, letters, korean text, speech bubble, watermark, signature`
- seed는 반드시 기록 (재현성)
- 실패 시 해당 컷만 실패 표시, 나머지 진행

### F-4. 말풍선 합성 — **[4] 수정 단계에 속한다**

생성 직후 자동 실행되지 않는다. 사용자가 `raw/` 그림을 보고 나서 실행한다.

**배치 규칙 (P-6)**

- **머리·얼굴 회피가 최우선이다 (P-7).** 후보 자리 중 인물의 머리와 겹치는 곳은 먼저 제외하고, 남은 자리에서 아래 규칙을 적용한다
- **자리 우선순위는 고정이다 (P-6).** 좌우 번갈아 계단식 하강:

  `left-upper`(y 0.03) → `right-upper`(0.26) → `left-lower`(0.50) → `right-lower`(0.73)

- **두 번째 이후 말풍선은 인물 머리보다 아래, 직전과 좌우 반대편.** 그래야 그림이 두 말풍선 사이에 놓인다
- 왼쪽 위가 머리로 막혀 있으면 **그 자리를 포기하고 다음 순위로 간다.** 겹치느니 비킨다
- 남은 자리가 하나도 없으면 그림 밖 띠로 밀어낸다 (나레이션과 같은 처리)
- 나레이션(`type: narration`)은 예외 — 그림 바깥 가로 띠, 좌측 정렬. 그림과 겹칠 수가 없다
- 말풍선 꼬리는 인물 쪽을 향하되, 꼬리 끝도 머리에 닿지 않는다
- `pos`가 명시되어 있으면 그 값이 우선한다 — 단 머리와 겹치면 경고를 남긴다

**머리 위치를 아는 방법**

배경 복잡도(엣지 밀도)만으로는 이 규칙을 지킬 수 없다. 배경이 복잡하고 인물이 단순한 컷에서는 오히려 얼굴 쪽이 "한산한 곳"으로 뽑힌다. 얼굴이 어디 있는지 실제로 알아야 한다.

| 수단 | 판단 |
|---|---|
| 얼굴 검출 (OpenCV Haar / MediaPipe) | 의존성이 가볍고 정면 얼굴에 강하다. **1순위** |
| 사람 검출 (YOLO) 후 상단 30%를 머리로 근사 | 뒷모습·측면에도 걸린다. 얼굴 검출이 놓칠 때의 보완 |
| 엣지 밀도 | 위 둘이 아무것도 못 찾았을 때의 최후 폴백 |

검출 결과는 `project.json`의 컷마다 `head_box`로 기록한다. 매 합성마다 다시 검출하지 않기 위해서, 그리고 사용자가 틀린 검출을 직접 고칠 수 있게 하기 위해서다.

**렌더링**

- **그림은 1080×1080 정사각 고정이다.** 어떤 비율로 생성했든 합성 단계에서 정사각으로 맞춘다.
  컷마다 그림 크기가 달라지면 캐러셀을 넘길 때 화면이 출렁인다
- 최종 캔버스는 1080×1350(4:5). 그림을 뺀 **270px이 띠 영역**이고, 나레이션은 위 / 자리 없어 밀려난 대사는 아래에 들어간다. 텍스트가 없는 컷도 위아래 여백을 균등히 남겨 그림 위치를 고정한다
- 생성 해상도도 정사각(1024×1024)으로 뽑는다. 세로로 생성하면 정사각 크롭에서 인물의 머리가 잘려나간다
- 한글 폰트 렌더링 (기본: 나눔고딕 / 배민 주아 등 상업적 이용 가능 폰트)
- 말풍선 종류: 대사(둥근 말풍선 + 꼬리), 나레이션(그림 밖 띠), 생각(타원 + 점 꼬리)
- 자동 줄바꿈 + 폰트 크기 자동 축소 (박스 초과 시)
- **대사가 길면 말풍선을 키우지 않고 여러 개로 쪼개 세로로 이어붙인다.**
  큰 덩어리 하나는 그림을 가리는 면적이 넓고 만화처럼 읽히지 않는다.
  - 2줄까지는 말풍선 하나, 3줄부터 쪼갠다
  - 조각들은 가운데를 맞추고 살짝 겹쳐 그린다 — 뒤 조각의 흰 배경이 앞 조각 테두리를 덮어 이어진 것처럼 보인다
  - **꼬리는 마지막 조각에만** 붙는다 (생각 말풍선의 점 꼬리도 마찬가지)
  - 조각마다 줄 수가 다르면 크기도 다르게 — 그게 자연스럽다
- **원본 `raw/` 이미지는 절대 덮어쓰지 않는다.** 합성 결과는 `out/`에만 쓴다 — 텍스트를 몇 번을 고쳐도 그림은 그대로 남아야 한다
- **폰트 라이선스는 상업적 이용 가능한 것만 번들** (인스타 게시 = 상업적 이용 가능성)

### F-5. 검수·수정 — 말풍선 삽입을 포함한 단계

컷 단위로 다음이 가능해야 한다.

| 동작 | 재실행 범위 | 비용 |
|---|---|---|
| **말풍선 최초 삽입** | 합성 (F-4) | 1초 |
| 대사 텍스트 수정 | 합성만 | 1초 |
| 말풍선 위치 변경 | 합성만 | 1초 |
| 다른 그림 원함 (같은 내용) | seed 변경 → 생성 + 합성 | GPU |
| 장면 자체 변경 | `scene_en` 수정 → 생성 + 합성 | GPU |
| 컷 순서 변경 | 파일명 재매핑만 | 즉시 |
| 컷 삭제 / 추가 | 인덱스 재정렬 | 즉시 |

**위 세 줄이 전체 작업 시간의 대부분을 차지한다.** 텍스트 다듬기는 GPU를 건드리지 않아야 한다.

- 잠금(`locked`) 컷은 일괄 재생성에서 제외
- 이전 버전 이미지는 덮어쓰지 않고 보존 (`raw/cut_05_v2.png`) — 되돌리기 대비

### F-6. 배포

- 출력 검증 (게시 전 필수)
  - 컷 수 ≤ 10
  - 모든 이미지 동일 종횡비, 지원 비율(1:1 또는 4:5)
  - 파일 크기·해상도 규격 충족
- 캡션 + 해시태그 조합 출력
- 게시 방식 2단계
  - **v1: 수동** — `out/` 폴더와 캡션 텍스트를 열어주고 사용자가 직접 업로드
  - **v2: 자동** — Instagram Content Publishing API로 캐러셀 게시

---

## 8. 인스타그램 배포 제약

> ⚠️ 아래 수치는 확인된 범위에서 기술했으나, **구현 착수 전 Meta 공식 문서로 재확인 필요**. 정책이 자주 바뀐다.

| 항목 | 제약 |
|---|---|
| 계정 요건 | 개인 계정 불가. **비즈니스 또는 크리에이터 계정** + 페이스북 페이지 연결 필요 |
| 캐러셀 장수 | 최대 10장 |
| 종횡비 | 1:1(1080×1080) 또는 4:5(1080×1350) 권장. 캐러셀은 **전 이미지 동일 비율 필수** |
| 게시 흐름 | 각 이미지 컨테이너 생성 → 캐러셀 컨테이너 생성 → 게시. **2단계 비동기** |
| 이미지 호스팅 | API는 **공개 URL**을 요구. 로컬 파일 직접 업로드 불가 → 임시 호스팅 필요 |
| 일일 게시 한도 | 계정당 24시간 한도 존재 (문서 확인 필요) |
| 게시 후 수정 | **캡션만 가능. 이미지 교체 불가** |

**"공개 URL 요구" 항목이 자동 배포의 숨은 비용이다.** 로컬에서 만든 PNG를 어딘가에 올려야 API가 읽어간다. 이 때문에 v1에서 자동 배포를 뺐다.

---

## 9. 기술 선택

### 이미지 생성 백엔드 — 둘을 함께 둔다

로컬 환경: **RTX 4060 (VRAM 8GB)**. C 드라이브 여유가 부족해 모델은 D 드라이브에 두고 ComfyUI 의 `extra_model_paths.yaml` 로 연결한다.

| 백엔드 | 쓸 때 | 대가 |
|---|---|---|
| **로컬 ComfyUI + SDXL** | 캐릭터가 특정되지 않은 편, 비용을 안 쓰고 싶을 때 | 캐릭터 일관성이 약하다 |
| **Gemini / Nano Banana** (`--backend gemini`) | **정해진 캐릭터로 그려야 할 때** | 컷당 과금 |

**MVP를 로컬로 시작한 판단은 유지하되, 캐릭터 일관성 때문에 API 백엔드를 더했다.**

M0 에서 확인한 것: 로컬 SDXL 로는 컷마다 캐릭터 형태가 무너진다. IP-Adapter 를 얹어도 마찬가지인데, 레퍼런스를 *이미지*로 참조해서 형태와 배경 구성이 한 다이얼에 묶이기 때문이다 — 강하게 밀면 배경이 사라지고 약하게 하면 형태가 깨진다. Nano Banana 는 레퍼런스를 *이 캐릭터*로 이해하므로 그 트레이드오프가 없다.

VRAM 8GB 에는 SDXL·ControlNet·IP-Adapter·CLIP Vision 을 동시에 못 올린다 (서버가 죽는다). 그래서 캐릭터 레퍼런스가 있으면 ControlNet 을 끈다 — 인물 위치 강제를 포기하고 캐릭터 스타일을 택한 것이다.

로컬 Flux 계열과 Flux Kontext 는 캐릭터 일관성이 뛰어나지만 전부 8GB 밖이라 후보에서 빠진다.

### 나머지 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 언어 | Python | ComfyUI·PIL 생태계 |
| 패키지 관리 | uv | (기존 환경 규약) |
| 컷 분할 LLM | `claude` CLI 헤드리스 | API 키 없이 동작한다 |
| 말풍선 합성 | Pillow | 별도 의존성 불필요 |
| 인물 위치 검출 | Gemini 좌표 질의 → Haar → 가정값 | 로컬 검출은 양식화된 캐릭터에서 전부 실패했다 (아래) |
| 검수 UI | **v1: CLI + 이미지 뷰어** → v2: 로컬 웹 | UI를 먼저 만들면 파이프라인이 늦어진다 |
| 상태 저장 | JSON 파일 | DB 불필요. 프로젝트 = 폴더 |

**DB 없음, 서버 없음, 계정 없음.** 1인용 로컬 도구로 시작한다.

### 인물 위치는 그림에서 되찾을 수 없다

말풍선이 인물을 피하려면(P-7) 인물이 어디 있는지 알아야 한다. 로컬로 여섯 가지를 시도했고 **전부 실패했다.**

| 방식 | 결과 |
|---|---|
| 엣지 밀도 | 배경(숲)이 더 복잡해서 인물을 "한산한 곳"으로 판정 |
| Haar 얼굴 | 실사풍은 잡지만 양식화된 캐릭터는 놓침 |
| 밝기 실루엣 | 안개·여백이 흰 캐릭터만큼 밝아 구분 안 됨 |
| YOLO 사람 | 8컷 전부 검출 0 — 2등신은 사람 형태가 아니다 |
| 원 검출 | 좌표가 화면 밖, 크기 전부 동일 = 오검출 |
| 닫힌 윤곽 | 8컷 중 1개만 정확 |

공통점은 **픽셀에서 "사람다움"을 찾으려 했다**는 것이고, 캐릭터가 양식화될수록 단서가 사라진다.

→ **그림을 이해하는 모델에게 직접 묻는다.** 생성된 컷을 보여주고 "주인공이 차지하는 영역"을 좌표로 받는다. 이미지를 만드는 게 아니라 읽는 것이라 비용이 낮고, 배경 인물과 주인공도 구분한다. 좌표는 `subject_box` 에 기록해 재합성 때는 다시 묻지 않는다.

---

## 10. 마일스톤

### M0 — 파이프라인 관통 ✅ 완료

원고 텍스트 → `out/` 폴더에 컷툰 PNG.

- [1] 해석·번역 (컷 분할 + 화풍·캐릭터 영어 변환) → `project.json` 생성
- [2] 한국어-영어 대조 출력 + 확인 게이트
- [3] 그림 생성 — 로컬 ComfyUI 또는 `--backend gemini`
- [4] Pillow 말풍선 합성 — **별도 명령으로 분리** (`render` → 확인 → `compose`)
- CLI: `instacut new` → `instacut render` → `instacut compose`

**M0 에서 확인한 것들** (설계 판단이 실제로 어땠는지)

| 예상 | 실제 |
|---|---|
| 캐릭터 일관성이 문제일 수 있다 | **심각했다.** 로컬 SDXL 로는 못 풀고 모델을 바꿔야 했다 |
| 프롬프트로 구도를 지시하면 된다 | 크기는 듣지만 **위치는 거의 안 듣는다** |
| 복잡도로 빈 자리를 찾으면 된다 | 배경이 더 복잡해서 반대로 작동했다 |
| 얼굴 검출로 회피하면 된다 | 실사풍만 된다. 양식화된 캐릭터는 로컬 검출 6종이 전부 실패 |

세 가지 모두 **"로컬에서 계산하려던 것을 이해하는 모델에게 맡기니 풀렸다"** 는 같은 패턴이었다.

### M1 — 컷 단위 수정

- `locked` 필드와 부분 재생성 (`instacut regen 5`, `instacut regen --all-unlocked`)
- 텍스트만 수정 시 합성만 재실행 (`instacut compose 3`)
- 말풍선 위치 미세 조정 (`pos` 값 수정 → 합성만)
- 버전 보존 (덮어쓰기 금지)
- 캐릭터 일관성 보강 — **M0에서 문자열 고정만으로 부족했을 때만.** 참조 이미지(IP-Adapter) 도입 검토

### M2 — 검수 UI

- 로컬 웹 페이지 (컷 그리드 뷰 + 컷 클릭 → 재생성/텍스트 수정)
- M1의 CLI 명령을 그대로 호출하는 얇은 껍데기

### M3 — 자동 배포

- 이미지 임시 호스팅 경로 확보
- Instagram Content Publishing API 연동
- 게시 전 규격 검증 게이트

---

## 11. 성공 지표

| 지표 | 목표 |
|---|---|
| 원고 → 완성 컷툰 소요 시간 | 30분 이내 (수동 작업 대비 단축) |
| 1회 생성 후 재생성 필요 컷 비율 | 30% 이하 |
| 캐릭터 일관성 (사람 판단) | 8컷 중 7컷 이상 "같은 인물로 보임" |
| 한글 대사 가독성 | 100% (합성 방식이므로 실패하면 버그) |
| **말풍선이 인물 머리와 겹친 컷** | **0개 (P-7 — 타협 없음)** |
| 머리 검출 성공률 | 8컷 중 7컷 이상 (실패 컷은 사용자가 `head_box` 직접 입력) |
| 게시 규격 검증 통과율 | 100% |

---

## 12. 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| ~~캐릭터 일관성이 문자열 고정만으로 안 나옴~~ | — | **해결.** 캐릭터 레퍼런스 + Gemini 백엔드 (IP-Adapter 로는 안 됐다) |
| **VRAM 8GB 한계** | 모델 조합 제약 | SDXL·ControlNet·IP-Adapter·CLIP Vision 을 동시에 못 올린다. 레퍼런스가 있으면 ControlNet 을 끈다 |
| **API 백엔드 비용** | 재생성이 잦으면 누적 | 좌표·컷 스크립트를 `project.json` 에 기록해 재합성은 공짜. 텍스트 수정은 GPU·API 를 쓰지 않는다 |
| **편마다 화풍이 달라 프롬프트 노하우가 축적되지 않음** | 첫 시도 품질 편차 | 잘 나온 편의 `project.json` 스타일 문자열을 복사 재사용 (별도 기능 불필요) |
| **한국어 인상어가 엉뚱하게 변환됨** ("담백한" → `bland`) | 의도와 다른 그림 8장 | F-2b 대조 출력 + 사용자 확인 게이트. 부록 B 예시를 [1] 프롬프트에 few-shot으로 주입 |
| 변환 시 원문에 없는 요소가 추가됨 (R-3 위반) | 예상 못 한 배경·소품 | 생성 전 1컷 시험 렌더로 확인 후 나머지 진행 |
| **모델이 왼쪽 여백 지시를 무시하고 인물을 중앙에 그림** | 말풍선 놓을 자리가 없음 | 실측 결과 이 지시는 거의 안 먹는다. 프롬프트에 기대지 말고 P-7(머리 회피)로 합성 단계에서 해결한다 |
| **머리 검출 실패 (뒷모습·측면·클로즈업)** | 말풍선이 얼굴을 덮음 | 얼굴→사람→엣지밀도 3단 폴백. 그래도 실패하면 `head_box`를 사용자가 직접 넣는다 |
| 얼굴이 프레임의 대부분을 차지하는 클로즈업 컷 | 피할 자리가 없음 | 그림 밖 띠로 밀어낸다. 대사가 짧으면 상단 띠 한 줄로 충분하다 |
| 대사가 길어 말풍선이 왼쪽 여백을 넘침 | 구도 파괴 | F-1 대사 길이 상한(40자) + F-4 폰트 자동 축소 |
| 말풍선이 그림의 중요 부분을 가림 | 품질 저하 | 프롬프트에 여백 영역 지시 + 대사 길이 상한 |
| 컷 분할 품질이 나쁨 (억지 분할) | 재작업 증가 | 컷 스크립트를 사용자가 생성 전에 검토·수정 |
| Instagram API 정책 변경 | M3 지연 | M0~M2가 수동 게시로도 완결되게 설계 (이미 반영) |
| 8GB VRAM 한계 | 모델 선택 제약 | SDXL 고정. 상위 모델 필요 시 클라우드로 전환 |
| 폰트 라이선스 위반 | 법적 문제 | 상업적 이용 가능 폰트만 번들. 목록 문서화 |
| AI 생성 이미지 표기 의무 | 계정 제재 가능성 | 캡션에 AI 생성 표기 옵션 기본 제공 |

---

## 13. 결정 사항 / 열린 질문

### 결정됨 (2026-08-14)

| 질문 | 결정 | 결과 |
|---|---|---|
| 연재인가 단발인가 | **단발** | 편 간 캐릭터 재사용·연재 관리 기능 제외. 일관성 요구는 한 편 내로 한정 |
| 화풍을 고정할 것인가 | **편마다 사용자가 문자열로 지정** | 스타일 프리셋·템플릿 시스템 제외. `--style` 인자 하나로 처리 |
| 화풍 입력 언어 | **한국어로 받아 [1]이 영어로 변환, 결과를 사용자에게 공유** | 번역 계층(P-5, F-2)이 추가되었으나 **LLM 호출은 늘지 않음** — 컷 분할과 같은 호출에 흡수 |

두 결정으로 M0에서 빠진 것: 캐릭터 시트 생성·승인 플로우, 프로젝트 간 시트 복사, 스타일 프리셋 관리. **M0이 실질적으로 파일 3개 규모로 줄었다.**

### 열린 질문

1. **원고는 어디서 오는가?** 파일/붙여넣기/기존 SNS 글 가져오기 — 입력 경로에 따라 [1] 단계 앞단이 달라진다.
2. **컷 수를 사람이 정하는가, 원고 길이에 맡기는가?**
3. **자동 배포가 실제로 필요한가?** 하루 1편이면 수동 업로드가 30초다. M3의 우선순위를 낮출 근거가 된다.

---

## 부록 A. M0 최소 실행 형태

```bash
# 프로젝트 생성 + 컷 분할
instacut new 원고.txt --cuts 8 --title "첫 출근 날" \
  --style "심플한 라인 드로잉, 파스텔 톤, 흰 배경"

# → 대조 출력 (F-2b)
#
#   화풍
#     한국어  심플한 라인 드로잉, 파스텔 톤, 감성적인 느낌
#     영어    clean minimal line art, thin uniform linework,
#             soft pastel palette, low saturation,
#             soft diffused lighting, generous white space
#
#   캐릭터  (원고에서 추출)
#     한국어  20대 여성, 단발, 검은 뿔테 안경, 베이지 코트
#     영어    young woman in her 20s, short bob haircut,
#             black horn-rimmed glasses, beige trench coat
#
#   8컷으로 분할됨. project.json 확인 후 render 하세요.

# 마음에 안 들면 project.json의 _en 값을 직접 수정 (재번역 안 함)

# [3] 그림만 생성 — 텍스트 없음, 왼쪽 3분의 1이 비어 있는 상태
instacut render
# → raw/cut_01.png ... cut_08.png

# 그림 확인. 왼쪽이 안 비었거나 마음에 안 드는 컷은 여기서 다시
instacut regen 5

# [4] 말풍선 얹기 — 왼쪽 위부터 시선 흐름대로 배치
instacut compose
# → out/cut_01.png ... cut_08.png

# 대사만 고치고 싶으면 project.json의 texts 수정 후 해당 컷만
instacut compose 3        # 1초. GPU 안 씀
```

파일 4개면 충분하다: `split.py`([1] 해석·번역), `render.py`([3] 그림), `compose.py`([4] 말풍선), `cli.py`.

---

## 부록 B. 한국어 → 영어 변환 예시

[1] 단계 프롬프트에 few-shot으로 주입할 대조표. **왼쪽이 아니라 오른쪽이 그림을 바꾼다.**

### 화풍 인상어

| 사용자 입력 | ❌ 직역 (효과 없음) | ✅ 시각 요소 분해 |
|---|---|---|
| 심플한 라인 드로잉 | `simple line drawing` | `clean minimal line art, thin uniform linework, flat colors, no shading` |
| 파스텔 톤 | `pastel tone` | `soft pastel palette, low saturation, muted colors, high key` |
| 감성적인 | `emotional` | `soft diffused lighting, warm color grading, muted palette, quiet composition` |
| 담백한 | `bland`, `plain` | `restrained composition, limited color palette, generous white space` |
| 웹툰 느낌 | `webtoon feeling` | `korean webtoon style, clean lineart, cel shading, digital coloring` |
| 낙서 같은 | `like a doodle` | `loose sketchy linework, hand-drawn imperfect strokes, marker texture` |
| 몽글몽글한 | `soft and fluffy` | `rounded shapes, soft edges, pastel gradients, gentle glow` |
| 쨍한 | `vivid` | `high saturation, strong contrast, bold flat color blocks` |
| 옛날 만화 같은 | `old cartoon style` | `retro halftone dots, limited 4-color print palette, slight paper grain` |

**패턴**: 인상어 하나가 선(linework) · 색(palette/saturation) · 빛(lighting) · 구도(composition) · 질감(texture) 중 2~4개 축으로 분해된다.

### 장면 서술

| 컷 내용 (한국어) | 영어 변환 |
|---|---|
| 지하철에서 긴장한 채 서 있다 | `standing in a crowded subway car, nervous tense expression, holding a handrail` |
| 사무실 문 앞에서 심호흡 | `standing in front of an office door, taking a deep breath, hand on chest` |
| 자리에 앉아 멍하니 모니터를 본다 | `sitting at a desk, blank vacant stare at a monitor, slumped posture` |

여기에 **F-3이 왼쪽 여백 지시를 자동으로 덧붙인다.** [1]이 쓸 필요는 없다:

```
subject positioned on the right side of the frame,
empty uncluttered space on the left third,
simple background on the left
```

**대사는 이 표에 없다.** 대사·나레이션은 한국어 원문 그대로 F-4가 렌더링한다 (F-1 규칙).

### 금지 사례 (R-3 위반)

| 입력 | 잘못된 변환 | 왜 |
|---|---|---|
| 파스텔 톤 | `soft pastel palette, cherry blossom background` | 배경은 원문에 없다. 8컷에 벚꽃이 깔린다 |
| 감성적인 | `emotional, crying, tears` | 감정 연출은 컷 내용이 정할 일이지 화풍이 정할 일이 아니다 |
| 심플한 | `simple, minimalist, white background, studio lighting, 8k, masterpiece` | 품질 태그 남발. 원문에 없는 지시가 컷 장면을 눌러버린다 |
