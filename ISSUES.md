# 알려진 문제와 남은 과제

M0 구현 중 실제로 부딪힌 것들. 추측이 아니라 돌려보고 확인한 내용이다.

---

## 하드웨어 제약

### VRAM 8GB — ControlNet 과 IP-Adapter 를 동시에 못 쓴다

SDXL(약 5GB) + ControlNet(2.5GB) + IP-Adapter + CLIP Vision(2.4GB)을 함께 올리면 **ComfyUI 서버가 죽는다.** ControlNet 만 얹었을 때도 VRAM 여유가 0.8GB까지 떨어져 스왑이 일어났고, 4컷 생성이 2분에서 20분으로 늘었다.

현재는 **캐릭터 레퍼런스가 있으면 ControlNet 을 끄는** 것으로 우회한다 (`render.FRAGMENT_CHAR`). 캐릭터 스타일을 위치 제어보다 우선한 선택이다.

이 우회의 대가로 **말풍선 자리 예약이 사라진다.** 캐릭터 레퍼런스를 쓰는 프로젝트에서는 인물 위치를 프롬프트로만 지시하므로, 말풍선이 인물을 덮을 수 있다. 합성 단계의 머리 회피(P-7)는 그대로 작동해 얼굴은 가리지 않는다.

- 해상도를 낮추면(768×960) 넷 다 올라갈 수도 있으나 검증하지 않았다
- `--lowvram` 모드는 모든 기능을 유지하지만 더 느려진다

### 디스크

C 드라이브 여유가 5GB뿐이라 모델은 D 드라이브에 둔다. ComfyUI 의 `extra_model_paths.yaml` 로 연결하며, 이 파일은 저장소 밖(ComfyUI 설치 폴더)에 있다.

```yaml
d_drive:
    base_path: D:/comfy-models/
    checkpoints: checkpoints
    controlnet: controlnet
    ipadapter: ipadapter
    clip_vision: clip_vision
```

---

## 생성 품질

### 프롬프트로는 구도가 제어되지 않는다

여러 번 시도해서 확인한 결과다.

| 지시 | 결과 |
|---|---|
| `왼쪽 3분의 1을 비워라` | 실패 |
| `인물은 화면 절반 높이` | 부분 성공 |
| `전신, 화면 1/3` | **성공** (크기는 조절된다) |
| `왼쪽 위 사분면을 비워라` | 실패 |

**크기는 듣지만 위치는 안 듣는다.** 그래서 ControlNet OpenPose 로 스틱 피규어를 그려 위치를 강제했고, 그건 한 번에 됐다. 다만 위의 VRAM 문제로 캐릭터 레퍼런스와 함께 쓸 수 없다.

### OpenPose 는 정면·뒷면을 구분하지 않는다

스틱 피규어에 앞뒤 정보가 없어 인물이 뒷모습으로 생성되는 경우가 있다. 얼굴이 안 보이면 감정이 전달되지 않고, 얼굴 검출도 실패해 말풍선 배치가 폴백으로 떨어진다.

→ 프롬프트에 `facing the viewer` 를 넣거나 `scene_en` 에서 `over-the-shoulder` 를 빼면 완화된다.

### 얼굴 검출(Haar)은 양식화된 그림에 약하다

실사풍 수채화에서는 잘 잡히지만, 플랫한 캐릭터 그림에서는 놓친다. 놓치면 "인물은 대개 가운데 위쪽에 있다"는 가정값으로 폴백하므로 얼굴을 덮지는 않지만, 자리 선택이 보수적으로 변한다.

→ `project.json` 의 `head_box` 를 직접 넣으면 그 값을 쓴다. 한 번 기록되면 재검출하지 않는다.

### 장면에 따라 인물이 여럿 나온다

"편의점에서 샀다" 같은 장면에서 점원이 함께 그려진 적이 있다. 캐릭터 문자열에 `solo` 를 넣어 완화했으나 완전하지는 않다.

### 캐릭터 일관성은 모델을 바꿔야 풀린다 (해결)

로컬 SDXL + IP-Adapter 로는 여러 강도를 시도해도 캐릭터 형태가 컷마다 무너졌다.
**Nano Banana(`--backend gemini`)로 바꾸니 한 번에 해결됐다.**

차이는 참조 방식이다. IP-Adapter 는 레퍼런스를 *이미지*로 참조해서 형태와 배경 구성이 한
다이얼에 묶여 있다 — 강하게 밀면 배경이 사라지고 약하게 하면 형태가 깨진다. Nano Banana 는
레퍼런스를 *이 캐릭터*로 이해하므로 형태를 유지한 채 새 장면에 배치한다.

대가는 컷당 과금이다. 로컬 경로는 그대로 남아 있으니 캐릭터가 중요하지 않은 편에는 계속 쓸 수 있다.

**응답 파싱 주의** — Gemini 응답에는 추론 단계(`type: thought`)가 함께 오고 그 안의
`signature` 가 989KB 짜리 base64 다. "가장 긴 문자열"을 이미지로 잡으면 깨진 파일을 받는다.
`mime_type` 이 `image/*` 인 블록만 골라야 한다 (`gemini.extract_image`).

### IP-Adapter 를 쓰면 해부학이 깨지기도 한다

캐릭터 레퍼런스를 강하게 밀면 눈이 셋이 되거나 팔다리가 늘어나는 경우가 있다. 레퍼런스의 형태를 억지로 맞추려다 생기는 것으로 보인다.

- `weight` 0.85 / `end_at` 0.9 — 캐릭터는 완벽하지만 **배경이 통째로 사라진다**
- `weight` 0.5 / `end_at` 0.45 — 배경과 소품이 일부 살아나지만 해부학 오류가 남는다

네거티브에 `three eyes, extra limbs, deformed` 를 넣으면 줄어들지만 완전히 막히지는 않았다.

### 이미지 모델은 글자를 못 쓴다

간판·표지판 글자가 뭉개진다. 네거티브 프롬프트로 막고 있지만 배경 소품까지는 완전히 통제되지 않는다. 대사·나레이션은 애초에 Pillow 로 합성하므로 영향 없다.

---

## 미구현

- **M2 검수 웹 UI** — 지금은 CLI + 탐색기로 확인한다
- **M3 인스타 자동 배포** — `out/` 을 수동 업로드한다. Instagram Content Publishing API 는 공개 URL 을 요구해서 임시 호스팅이 필요하다
- **컷 순서 변경 / 삭제·추가** — `project.json` 을 직접 고쳐야 한다
- **편별 스타일 프리셋** — 매번 `--style` 로 지정한다 (의도된 설계, PRD 13절)

---

## 운영상 주의

### ComfyUI 서버가 자주 내려간다

세션 사이에 종료되고, comfy-cli 가 죽은 프로세스를 "실행 중"으로 기억하는 stale 상태에 빠진다. 그때는 `comfy stop` 으로 정리한 뒤 다시 띄워야 한다.

```bash
comfy stop
comfy launch --background
```

### 조용한 실패에 주의

`render` 가 실패해도 `compose` 는 이전 `raw/` 로 성공한다. 결과가 안 바뀌는데 명령은 성공한 것처럼 보인다. IPAdapter preset 과 실제 받은 모델이 어긋나 두 번이나 이렇게 놓쳤다.

→ `render` 에 `comfy validate` 사전 검증을 넣어 제출 전에 잡도록 했다.

### PowerShell 로 project.json 을 만지지 말 것

`Out-File -Encoding utf8` 이 BOM 을 붙여 JSON 이 깨진다. 코드는 `utf-8-sig` 로 읽어 견디지만, 쓸 때는 파이썬을 쓰는 편이 안전하다.
