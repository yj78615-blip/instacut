# 알려진 문제와 남은 과제

M0 구현 중 실제로 부딪힌 것들. 추측이 아니라 돌려보고 확인한 내용이다.

---

## 하드웨어 제약

### VRAM 8GB — ControlNet 과 IP-Adapter 를 동시에 못 쓴다

SDXL(약 5GB) + ControlNet(2.5GB) + IP-Adapter + CLIP Vision(2.4GB)을 함께 올리면 **ComfyUI 서버가 죽는다.** ControlNet 만 얹었을 때도 VRAM 여유가 0.8GB까지 떨어져 스왑이 일어났고, 4컷 생성이 2분에서 20분으로 늘었다.

현재는 **캐릭터 레퍼런스가 있으면 ControlNet 을 끄는** 것으로 우회한다 (`render.FRAGMENT_CHAR`). 캐릭터 스타일을 위치 제어보다 우선한 선택이다.

이 우회의 대가로 **말풍선 자리 예약이 사라진다.** 캐릭터 레퍼런스를 쓰는 프로젝트에서는 인물 위치를 프롬프트로만 지시하므로, 말풍선이 인물을 덮을 수 있다. 합성 단계의 머리 회피(P-7)는 그대로 작동해 얼굴은 가리지 않는다.

- 해상도를 낮추면(768×960) 넷 다 올라갈 수도 있으나 검증하지 않았다
- `--lowvram` 모드는 모든 기능을 유지하지만 더 느려진다

### 디스크

C 드라이브 여유가 5GB뿐이라 모델은 D 드라이브에 둔다. ComfyUI 의 `extra_model_paths.yaml` 로 연결하며, 이 파일은 저장소 밖(ComfyUI 설치 폴더)에 있다.

```yaml
d_drive:
    base_path: D:/comfy-models/
    checkpoints: checkpoints
    controlnet: controlnet
    ipadapter: ipadapter
    clip_vision: clip_vision
```

---

## 생성 품질

### 프롬프트로는 구도가 제어되지 않는다 (SDXL 한정)

**이 절은 로컬 SDXL 백엔드에 대한 것이다.** Nano Banana 에서는 같은 지시가 작동한다 —
편의점 4컷에서 `subject positioned on the right side of the frame, upper left quadrant
completely empty` 가 그대로 지켜졌다. 자세한 근거와 원인은 [COMPOSITION.md](COMPOSITION.md) 참고.

여러 번 시도해서 확인한 결과다.

| 지시 | 결과 |
|---|---|
| `왼쪽 3분의 1을 비워라` | 실패 |
| `인물은 화면 절반 높이` | 부분 성공 |
| `전신, 화면 1/3` | **성공** (크기는 조절된다) |
| `왼쪽 위 사분면을 비워라` | 실패 |

**크기는 듣지만 위치는 안 듣는다.** 그래서 ControlNet OpenPose 로 스틱 피규어를 그려 위치를 강제했고, 그건 한 번에 됐다. 다만 위의 VRAM 문제로 캐릭터 레퍼런스와 함께 쓸 수 없다.

### OpenPose 는 정면·뒷면을 구분하지 않는다

스틱 피규어에 앞뒤 정보가 없어 인물이 뒷모습으로 생성되는 경우가 있다. 얼굴이 안 보이면 감정이 전달되지 않고, 얼굴 검출도 실패해 말풍선 배치가 폴백으로 떨어진다.

→ 프롬프트에 `facing the viewer` 를 넣거나 `scene_en` 에서 `over-the-shoulder` 를 빼면 완화된다.

### 인물 위치 검출 — 로컬 방식은 전부 실패했다 (해결)

말풍선이 인물을 피하려면 인물이 어디 있는지 알아야 한다. 로컬로 여섯 가지를 시도했고 **전부 실패했다.**

| 방식 | 결과 |
|---|---|
| 엣지 밀도 | 배경(숲·나뭇잎)이 더 복잡해서 인물을 "한산한 곳"으로 판정 |
| Haar 얼굴 | 실사풍은 잡지만 양식화된 캐릭터는 놓침 |
| 밝기 실루엣 | 안개·종이 여백이 흰 캐릭터만큼 밝아 구분 안 됨 |
| YOLO 사람 | 8컷 전부 검출 0 — 2등신 캐릭터는 사람 형태가 아니다 |
| 원 검출(Hough) | 좌표가 화면 밖으로 나가고 크기가 전부 동일 = 오검출 |
| 닫힌 윤곽 | 8컷 중 1개만 정확, 배경 나무와 구별 못 함 |

공통점은 **픽셀에서 "사람다움"을 찾으려 했다**는 것이고, 캐릭터가 양식화될수록 단서가 사라진다.

**해결: 그림을 이해하는 모델에게 직접 묻는다** (`gemini.locate_subject`).
생성된 컷을 보여주고 "주인공이 차지하는 영역"을 좌표로 받는다. 이미지를 만드는 게 아니라
읽는 것이라 비용이 훨씬 낮고, **배경 인물과 주인공도 구분한다**(지하철 컷에서 확인).

좌표는 `project.json` 의 `subject_box` 에 기록되어 재합성 때는 다시 묻지 않는다.
API 키가 없으면 Haar → 가정값 순으로 자동으로 내려간다.

### 얼굴 검출(Haar)은 양식화된 그림에 약하다

실사풍 수채화에서는 잘 잡히지만, 플랫한 캐릭터 그림에서는 놓친다. 놓치면 "인물은 대개 가운데 위쪽에 있다"는 가정값으로 폴백하므로 얼굴을 덮지는 않지만, 자리 선택이 보수적으로 변한다.

→ `project.json` 의 `head_box` 를 직접 넣으면 그 값을 쓴다. 한 번 기록되면 재검출하지 않는다.

### 좌표를 배열로 받으면 축이 뒤바뀐다 (해결)

인물 위치를 `{"box": [x0, y0, x1, y1]}` 로 달라고 했더니 모델이 자기 관례인
`[y, x, y, x]` 순서로 답했다. 값의 범위(0~1)는 맞아서 파싱은 통과하고,
**말풍선 꼬리만 엉뚱한 곳을 가리켰다.**

편의점 컷2에서 점원은 말풍선 바로 아래에 있는데 꼬리가 오른쪽으로 수평하게 뻗었다.
축을 바꿔 계산하면 그 방향과 정확히 맞아떨어져 원인이 잡혔다.

배열의 순서는 모델이 지킬 의무가 없는 암묵적 약속이다. **필드 이름은 뒤바뀔 수 없다.**

```json
{"people": [{"label": "점원", "x0": 0.13, "y0": 0.37, "x1": 0.28, "y1": 0.68}]}
```

`gemini.parse_box` / `parse_people` 은 예전 배열 형식도 계속 받아준다(기존 `project.json` 호환).
자체 검사에 "점원이 주인공보다 왼쪽"이라는 비교를 넣어, 축이 다시 뒤집히면 바로 깨진다.

### 모델이 후보 밖 라벨을 답한다 (해결)

인물 위치를 물을 때 역할 후보(`점원, 주인공`)를 프롬프트로 주는데도 `손님` 처럼
후보를 벗어난 이름이 돌아온다. `people.get("주인공")` 이 빗나가 꼬리가 엉뚱한 곳을 겨눴다.

지시를 강화하는 것으로는 못 막는다 — **모델 출력에 대한 방어가 코드에 있어야 한다.**
`parse_people(text, roles)` 가 응답 라벨을 후보 이름으로 정규화한다.

1. 정확히 일치하는 라벨을 먼저 가져간다
2. 남은 것끼리 **개수가 정확히 맞을 때만** 순서대로 배정한다
3. 개수가 안 맞으면 배정하지 않는다

3번이 중요하다. 처음에는 남은 후보 중 첫 번째에 붙였는데, 응답이 후보보다 적으면
(모델이 한 명을 놓치는 일이 실제로 있다) **누가 빠졌는지 모르는 채로 엉뚱한 사람에게**
꼬리를 건다. 비워두면 호출자가 주인공 상자로 폴백하므로 그쪽이 안전하다.

**모르면 추측하지 말고 비워둔다** — 틀린 값보다 없는 값이 낫다.

### 화자가 한 명이어도 주인공이 아니면 물어야 한다 (해결)

인물 위치 질의를 **화자가 둘 이상일 때만** 걸었다. 그래서 점원 혼자 말하는 컷에서
꼬리가 주인공을 겨눴다. 조건을 `speakers - {"주인공"}` 으로 바꿨다 — 주인공 혼자
말하는 컷은 이미 `subject_box` 를 알고 있으니 물을 필요가 없고, 나머지는 다 물어야 한다.

### 장면에 따라 인물이 여럿 나온다

"편의점에서 샀다" 같은 장면에서 점원이 함께 그려진 적이 있다. 캐릭터 문자열에 `solo` 를 넣어 완화했으나 완전하지는 않다.

### 캐릭터 일관성은 모델을 바꿔야 풀린다 (해결)

로컬 SDXL + IP-Adapter 로는 여러 강도를 시도해도 캐릭터 형태가 컷마다 무너졌다.
**Nano Banana(`--backend gemini`)로 바꾸니 한 번에 해결됐다.**

차이는 참조 방식이다. IP-Adapter 는 레퍼런스를 *이미지*로 참조해서 형태와 배경 구성이 한
다이얼에 묶여 있다 — 강하게 밀면 배경이 사라지고 약하게 하면 형태가 깨진다. Nano Banana 는
레퍼런스를 *이 캐릭터*로 이해하므로 형태를 유지한 채 새 장면에 배치한다.

대가는 컷당 과금이다. 로컬 경로는 그대로 남아 있으니 캐릭터가 중요하지 않은 편에는 계속 쓸 수 있다.

**응답 파싱 주의** — Gemini 응답에는 추론 단계(`type: thought`)가 함께 오고 그 안의
`signature` 가 989KB 짜리 base64 다. "가장 긴 문자열"을 이미지로 잡으면 깨진 파일을 받는다.
`mime_type` 이 `image/*` 인 블록만 골라야 한다 (`gemini.extract_image`).

### IP-Adapter 를 쓰면 해부학이 깨지기도 한다

캐릭터 레퍼런스를 강하게 밀면 눈이 셋이 되거나 팔다리가 늘어나는 경우가 있다. 레퍼런스의 형태를 억지로 맞추려다 생기는 것으로 보인다.

- `weight` 0.85 / `end_at` 0.9 — 캐릭터는 완벽하지만 **배경이 통째로 사라진다**
- `weight` 0.5 / `end_at` 0.45 — 배경과 소품이 일부 살아나지만 해부학 오류가 남는다

네거티브에 `three eyes, extra limbs, deformed` 를 넣으면 줄어들지만 완전히 막히지는 않았다.

### 이미지 모델은 글자를 못 쓴다

간판·표지판 글자가 뭉개진다. 네거티브 프롬프트로 막고 있지만 배경 소품까지는 완전히 통제되지 않는다. 대사·나레이션은 애초에 Pillow 로 합성하므로 영향 없다.

---

## 미구현

- **M2 검수 웹 UI** — 지금은 CLI + 탐색기로 확인한다.
  수정 자체는 `instacut edit` 으로 되지만(대사·자리·꼬리), 결과를 보려면 탐색기에서 파일을 다시 열어야 한다
- **M3 인스타 자동 배포** — `instacut export` 까지 하고 업로드는 사람이 한다.
  Instagram Content Publishing API 는 공개 URL 을 요구해서 임시 호스팅이 필요하다
- **컷 순서 변경 / 삭제·추가** — `project.json` 을 직접 고쳐야 한다
- **캡션·해시태그** — `project.json` 에 필드만 있고 채우지 않는다
- **편별 스타일 프리셋** — 매번 `--style` 로 지정한다 (의도된 설계, PRD 13절)

---

## 운영상 주의

### ComfyUI 서버가 자주 내려간다

세션 사이에 종료되고, comfy-cli 가 죽은 프로세스를 "실행 중"으로 기억하는 stale 상태에 빠진다. 그때는 `comfy stop` 으로 정리한 뒤 다시 띄워야 한다.

```bash
comfy stop
comfy launch --background
```

### 조용한 실패에 주의

`render` 가 실패해도 `compose` 는 이전 `raw/` 로 성공한다. 결과가 안 바뀌는데 명령은 성공한 것처럼 보인다. IPAdapter preset 과 실제 받은 모델이 어긋나 두 번이나 이렇게 놓쳤다.

→ `render` 에 `comfy validate` 사전 검증을 넣어 제출 전에 잡도록 했다.

### 오진을 자체 검사에 못박지 말 것

생각 말풍선의 점이 배경 나무 쪽을 향한 적이 있다. 원인은 **엉뚱한 대상을 겨눈 것**인데
길이 문제로 읽고 점을 인물 얼굴까지 늘렸다. 그리고 그 결론을 자체 검사로 굳혔다.

```python
assert TAIL_DOTS[-1] * TAIL_REACH > 0.85, "꼬리가 인물까지 닿지 않습니다"
```

요구는 "향한다"였지 "닿는다"가 아니었다. 이 검사가 남아 있는 동안 꼬리를 짧게 해달라는
요청이 들어올 때마다 검사가 반대 방향으로 밀었고, 점이 얼굴을 덮는 문제까지 생겼다.

지금은 뒤집혀 있다 — 말풍선 바로 아래 점 세 개로 방향만 가리키고 인물까지 뻗지 않는다.

**검사는 확인된 규칙만 담는다.** 원인을 추정해서 넣은 검사는 다음 사람(혹은 다음 세션의
자신)에게 그 추정을 사실로 전달한다.

### 그림을 좌우로 뒤집지 않는다 (해결)

말풍선 왼쪽 위 우선(P-6)을 지키려고, 인물이 왼쪽에 서면 `compose_cut` 이 그림을 통째로
뒤집었다. 코드 주석에 **"그림에 글자가 없으니 어색하지 않다"** 는 전제가 적혀 있었는데,
간판이 나오는 순간 깨졌다 — `STORE` 가 거울상으로 찍혔다.

`_pick_zone` 이 머리와 겹치는 자리를 건너뛰므로 인물이 왼쪽이면 알아서 오른쪽 위를 고른다.
뒤집을 이유가 없었다. 다섯 줄을 지우자 좌표계가 한 겹 줄어 자리 판정도 정확해졌다.

**전제를 주석에 적어두면 그것이 깨지는 날 찾을 수 있다.** 이 건은 그래서 원인이 빨리 잡혔다.

### PowerShell 로 project.json 을 만지지 말 것

`Out-File -Encoding utf8` 이 BOM 을 붙여 JSON 이 깨진다. 코드는 `utf-8-sig` 로 읽어 견디지만, 쓸 때는 파이썬을 쓰는 편이 안전하다.
